<!--pages/my-classes/components/BookingCard/BookingCard.wxml-->

<!-- <view class='card-my-classes' style='color: white !important'>
  <view class='block'>
    <view class='bold' style="flex-direction: column">
      <view class='text-size-sheader' style='margin-top: 10px;'>{{booking.session.name}} </view>
      <view class="" style="margin-top: 15px; ">
        <view style="font-size: 16px;">
          <image src='/images/time-white.png' class='icon-time-booking'> </image> {{booking.session.duration}} min
        </view>
      </view>
      <view class="w100 d-flex" style='margin-top: 10px; margin-bottom: 5px; color: white; '>
        <view class='text14 mr'>{{booking.session.date}} </view>
        <view class='text14' style='margin-left: 20px'>{{booking.session.from}} - {{booking.session.to}}</view>
      </view>
    </view>

    <image class="card-image-my-classes" src="{{booking.session.image_url}}"></image>
  </view>

  <view class="status {{booking.attended ? 'green-status' : 'red-status'}}">
    {{booking.attended ? 'COMPLETED' : 'NO SHOW'}}</view>


</view> -->

<view class="class-card">
  <!-- card -->
  <view class="bold d-flex space-between pa">
    <!-- left side -->
    <view class="d-flex flex-column space-between flex-grow">
      <!-- header -->
      <view class="text-size-sheader">{{booking.session.name}}</view>
      <!-- header ends -->
      <!-- duration -->
      <view class="text14">
        <image src='/images/time-white.png' class='icon-time-booking'> </image> {{booking.session.duration}} min
      </view>
      <!-- duration ends -->
      <!-- date time -->
      <view class="d-flex space-between text14">
        <view>{{booking.session.date}}</view>
        <view>{{booking.session.from}} - {{booking.session.to}}</view>
      </view>
      <!-- date time ends -->
    </view>
    <!-- left side ends -->
    <!-- image -->
    <image class="class-image" src="{{booking.session.image_url}}"></image>
    <!-- image ends -->
  </view>
  <!-- card ends -->
  <view wx:if="{{selected == 1}}" class="status {{booking.attended ? 'green-status' : 'red-status'}}">
    {{booking.attended ? 'COMPLETED' : 'NO SHOW'}}
  </view>
</view>