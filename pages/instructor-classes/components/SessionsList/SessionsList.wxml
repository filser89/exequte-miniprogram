<!--pages/instructor-classes/components/SessionsList/SessionsList.wxml-->
<view wx:for="{{sessions}}" wx:key="unique">
  <view>
    <SessionCard studio="{{studio}}" isUserAdmin="{{isUserAdmin}}" strings="{{strings}}" session="{{item}}" selected="selected" />
  </view>
</view>
