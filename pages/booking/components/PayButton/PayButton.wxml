<!--pages/booking/components/PayButton/PayButton.wxml-->
<view class="text-capitalize text-white btn-container">
  <view class="action-btn"  data-action="{{action}}"
    disabled="{{btnDisabled}}" catchtap="takeAction">
    <slot />
  </view>
  <!-- <slot /> -->
</view>
