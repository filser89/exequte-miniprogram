<!--components/ActionButtonSmall/ActionButtonSmall.wxml-->
<view class="class-info-book-button">
  <view class="{{btnDisabled ? 'action-btn-disabled' : 'action-btn'}} {{studio}}-action-btn" data-action="{{action}}" disabled="{{btnDisabled}}" catchtap="takeAction" >
  <slot />
   </view>
</view>

